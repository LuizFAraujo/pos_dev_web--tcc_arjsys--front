/**
 * almoxarifadoRegistry.ts - Registry do módulo Almoxarifado
 *
 * Registra páginas do setor Almoxarifado (futuro):
 * - Estoque
 * - Movimentações
 */

import type { TabRegistry } from '@/types/registry.types';

export const almoxarifadoRegistry: TabRegistry = {
  // Backend ainda não implementado
};
