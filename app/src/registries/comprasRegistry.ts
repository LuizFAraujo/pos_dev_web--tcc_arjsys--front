/**
 * comprasRegistry.ts - Registry do módulo Compras
 * 
 * Registra todas as páginas do módulo de compras:
 * - Requisições de Compra (lista e cadastro)
 */

import type { TabRegistry } from '@/types/registry.types';

export const comprasRegistry: TabRegistry = {
    // Será preenchido posteriormente
};