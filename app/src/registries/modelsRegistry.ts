/**
 * modelsRegistry.ts - Registry de páginas modelo
 * 
 * Registra as páginas modelo (templates) que servirão de exemplo:
 * - Modelo Lista Simples
 * - Modelo Formulário
 * - Modelo Página Complexa
 */

import type { TabRegistry } from '@/types/registry.types';

export const modelsRegistry: TabRegistry = {
    // Será preenchido posteriormente
};