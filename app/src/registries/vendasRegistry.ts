/**
 * vendasRegistry.ts - Registry do módulo Vendas
 * 
 * Registra todas as páginas do módulo de vendas:
 * - Pedidos de Venda (lista e cadastro)
 */

import type { TabRegistry } from '@/types/registry.types';

export const vendasRegistry: TabRegistry = {
    // Será preenchido posteriormente
};